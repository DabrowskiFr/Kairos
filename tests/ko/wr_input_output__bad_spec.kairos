node wr_input_output(x: int) returns (y: int)
contracts
  requires: G(x = 0 or x = 1);
  ensures: G (undefined_spec_symbol = 0);
states
  Init(init), Run;
transitions
  Init:
    to Run {
      y := x;
    }
  Run:
    to Run {
      y := x;
    }
end
