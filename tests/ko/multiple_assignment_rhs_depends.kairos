node multiple_assignment_rhs_depends(x: int) returns (
  a: int, b: int
)
contracts
  ensures: X G(a >= 0);
states
  Init(init), Run;
transitions
  Init:
    to Run {
      a, b := b, a;
    }
  Run:
    to Run {
      a, b := x, x + 1;
    }
end
