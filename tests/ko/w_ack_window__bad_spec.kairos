node w_ack_window(req: int) returns (ack: int)
contracts
  requires: G(req = 0 or req = 1);
  ensures: X G((req = 1) => ((ack = 2) W (req = 0)));
states
  Init(init), Run;
transitions
  Init:
    to Run {
      ack := req;
    }
  Run:
    to Run {
      ack := req;
    }
end
