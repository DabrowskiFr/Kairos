node init_state_invariant() returns (x: int)
contracts
  ensures: X G(x >= 0);
states
  Init(init), Run;
invariants
  in Init:
    x >= 0;
transitions
  Init:
    to Run {
      x := 0;
    }
  Run:
    to Run {
      x := x + 1;
    }
end
