node ghost_bad_guard(input: bool) returns (
  out: bool
)
ghosts
  proofFlag: bool;

contracts
  ensures: G(out = false);

states Init(init);

trans
  Init:
    to Init when proofFlag {
      out := false;
      proofFlag := input;
    }
end
