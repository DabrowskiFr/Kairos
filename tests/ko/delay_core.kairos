node delay_core(x: int) returns (outv: int)
contracts
  ensures: X ( G (outv = prev x) );
locals
  z : int;
states
  Init(init), Run;
invariants
  in Run:
    z = prev x;
transitions
  Init:
    to Run {
    outv := 0;
    z := x;
  }
  Run:
    to Run {
    outv := z;
    z := x;
  }
end
