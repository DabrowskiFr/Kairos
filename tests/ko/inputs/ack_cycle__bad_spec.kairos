node ack_cycle(req : int) returns (ack: int)
contracts
  ensures: G ((ack = ack) and not (ack = ack));
states
  Init(init);
transitions
  Init:
    to Init  {
      if req = 1 or ack = 1 then ack := 1;
      else skip; end;
    }
end
