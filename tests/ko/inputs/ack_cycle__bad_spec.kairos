node ack_cycle(req : int) returns (ack: int)
contracts
  ensures: G (undefined_spec_symbol = 0);
states
  Init(init);
transitions
  Init:
    to Init  {
      if req = 1 or ack = 1 then ack := 1;
      else skip; end;
    }
end
