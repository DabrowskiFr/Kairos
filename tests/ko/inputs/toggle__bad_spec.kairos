node toggle() returns (y: int)
contracts
  ensures: G (undefined_spec_symbol = 0);
states
  Init(init), Run;
transitions
  Init:
    to Run  {
    y := 0;
  }
  Run:
    to Run  {
    y := 1 - y;
  }
end
