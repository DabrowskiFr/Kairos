node toggle() returns (y: int)
contracts
  ensures: G ((y = y) and not (y = y));
states
  Init(init), Run;
transitions
  Init:
    to Run  {
    y := 0;
  }
  Run:
    to Run  {
    y := 1 - y;
  }
end
