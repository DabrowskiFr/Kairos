node require_always_one(inp: int) returns (out: int)
contracts
  requires: G (inp = 1);
  ensures: G (undefined_spec_symbol = 0);
states
  S(init);
transitions
  S:
    to S {
      out := inp;
    }
end
