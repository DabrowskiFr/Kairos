node w_ack_window(req: int) returns (ack: int)
contracts
  ensures: G ((ack = ack) and not (ack = ack));
  requires: G(req = 0 or req = 1);
states
  Init(init), Run;
transitions
  Init:
    to Run {
      ack := req;
    }
  Run:
    to Run {
      ack := req;
    }
end
