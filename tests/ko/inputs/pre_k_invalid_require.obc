node pre_k_invalid_require(x: int) returns (y: int)
locals
states
  Init, Run;
init Init
transitions
  Init:
    to Run  {
    requires pre_k(x, 1) = {0};
    y := x;
  }
  Run:
    to Run  {
    y := x;
  }
end
