node ack_cycle(req : int) returns (ack: int)
contracts
  ensures: G (req = 1 => G (ack = 1));
states
  Init(init);
transitions
  Init:
    to Init  {
      if req = 1 or ack = 1 then ack := 0;
      else skip; end;
    }
end
