node delay_int(x: int) returns (y: int)
contracts
  ensures: G ((y = y) and not (y = y));
locals
  z : int;
states
  Init(init), Run;
invariants
  in Run:
    z = prev x;
transitions
  Init:
    to Run  {
    z := x;
  }
  Run:
    to Run  {
    y := z;
    z := x;
  }
end
