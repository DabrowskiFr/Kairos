node delay_int(x: int) returns (y: int)
contracts
  ensures: X ( G (y =  prev x) );
locals
  z : int;
states
  Init(init), Run;
invariants
  in Run:
    z = prev x;
transitions
  Init:
    to Run  {
    z := x;
  }
  Run:
    to Run  {
    y := 0;
    z := x;
  }
end
