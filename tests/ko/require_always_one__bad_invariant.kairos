node require_always_one(inp: int) returns (out: int)
contracts
  requires: G (inp = 1);
  ensures: G (out = 1);
states
  S(init);
invariants
  in S:
    0 = 1;
transitions
  S:
    to S {
      out := inp;
    }
end
