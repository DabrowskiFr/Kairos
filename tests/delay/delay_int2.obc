node delay_int2(x: int) returns (y: int)
guarantee G( {y} = pre_k(x, 2) );
locals
  prev1: int;
  prev2: int;
states
  Init1, Init2, Run;
init Init1
trans
  Init1 -> Init2 {
    ensures {prev1} = {x};
    ensures {prev2} = {0};
    y := 0;
    prev1 := x;
    prev2 := 0;
  }
  Init2 -> Run {
    ensures {prev2} = pre(prev1);
    ensures {prev1} = {x};
    y := 0;
    prev2 := prev1;
    prev1 := x;
  }
  Run -> Run {
    ensures {prev2} = pre(prev1);
    ensures {prev1} = {x};
    y := prev2;
    prev2 := prev1;
    prev1 := x;
  }
end
