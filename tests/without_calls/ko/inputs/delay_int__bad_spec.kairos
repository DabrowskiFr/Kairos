node delay_int(x: int) returns (y: int)
contracts
  ensures: G (undefined_spec_symbol = 0);
locals
  z : int;
states
  Init(init), Run;
invariants
  in Run:
    z = prev x;
transitions
  Init:
    to Run  {
    z := x;
  }
  Run:
    to Run  {
    y := z;
    z := x;
  }
end
