node toggle() returns (y: int)
guarantee {y} = {0};
guarantee G( {y} = {0} => X( {y} = {1} ) );
guarantee G( {y} = {1} => X( {y} = {0} ) );
locals
states
  Init, Run;
init Init
trans
  Init -> Run {
    y := 0;
  }
  Run -> Run {
    y := 1 - y;
  }
end
