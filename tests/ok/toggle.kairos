node toggle() returns (y: int)
contracts
  ensures: y = 0;
  ensures: G ( y = 0 => X ( y = 1 ) );
  ensures: G ( y = 1 => X ( y = 0 ) );
states
  Init(init), Run;
transitions
  Init:
    to Run  {
    y := 0;
  }
  Run:
    to Run  {
    y := 1 - y;
  }
end
