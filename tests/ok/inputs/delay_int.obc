node delay_int(x: int) returns (y: int)
contracts
  ensures: next always (y =  prev x) ;
locals
  s : int;
states
  Init(init), Run;
invariants
  in Run:
    s = prev x;
transitions
  Init:
    to Run  {
    y := 0;
    s := x;
  }
  Run:
    to Run  {
    y := s;
    s := x;
  }
end
