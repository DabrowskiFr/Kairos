node toggle_if() returns (y: int)
guarantee G( {y} = {0} => X( {y} = {1} ) );
locals
states
  Init, Run;
init Init
trans
  Init -> Run {
    y := 0;
  }
  Run -> Run {
    if y = 0 then
      y := 1;
    else
      y := 0;
    end;
  }
end
