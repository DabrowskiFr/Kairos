node safe_mode() returns (mode: int, safe: int)
guarantee G( {mode} = {1} => G( {safe} = {1} ) );
guarantee G(G( {mode} = {1} => G( {safe} = {1} ) ));
locals
states
  Safe;
init Safe
trans
  Safe -> Safe {
    requires {mode} = {1};
    requires {safe} = {1};
    mode := 1;
    safe := 1;
  }
end
