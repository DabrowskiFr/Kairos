opam-version: "2.0"
name: "kairos-lsp"
version: "dev"
synopsis: "Language Server Protocol adapter for Kairos"
description: "Optional JSON-RPC/LSP delivery adapter embedding the public Kairos engine facade."
depends: [
  "dune" {>= "3.10"}
  "ocaml"
  "kairos" {= version}
  "kairos-graphviz-adapter" {= version}
  "yojson"
  "jsonrpc"
  "lsp"
]
build: [
  ["dune" "build" "-p" name "-j" jobs "@install"]
]
install: [
  ["dune" "install" "-p" name "--create-install-files" "--prefix" prefix "--libdir" lib]
]
