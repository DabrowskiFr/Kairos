(executable
 (name main)
 (public_name obc2why3)
(modules
  ast
  lexer
  parser
  whygen_support
  whygen_collect
  whygen_passes
  whygen_compile_expr
  whygen_emit
  whygen_monitor_transform
  whygen_dot
  whygen_monitor_emit
  whygen
  whygen_specs
  whygen_automaton_core
  main)
 (libraries str re why3)
 (preprocess (pps ppx_deriving.show)))

(include_subdirs no)

(ocamllex lexer)

(menhir
 (modules parser))
