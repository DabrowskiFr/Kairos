(library
 (name obcwhy3_lib)
 (wrapped false)
 (modules
  ast
  lexer
  parser
  support
  collect
  contract_link
  compile_expr
  emit_obc
  emit_why_types
  emit_why_env
  emit_why_contracts
  emit_why_diagnostics
  emit_why_core
  emit
  monitor_transform
  dot
  monitor_emit
  specs
  time_shit
  automaton_util
  automaton_config
  automaton_atoms
  automaton_naive
  automaton_valuation
  automaton_bdd
  automaton_ltl
  automaton_types
  automaton_residual
  automaton_core)
 (libraries str re why3)
 (preprocess (pps ppx_deriving.show)))

(executable
 (name main)
 (public_name obc2why3)
 (modules main)
 (libraries obcwhy3_lib))

(include_subdirs unqualified)
